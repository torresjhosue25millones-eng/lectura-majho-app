{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm --prefix backend ci && npm --prefix frontend ci && npm run build --prefix backend && npm run build --prefix frontend && cp -r frontend/dist public && cd backend && npx playwright install --with-deps chromium"
  },
  "deploy": {
    "startCommand": "node backend/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
